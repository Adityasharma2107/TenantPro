# TenantPro Client Environment Variables
# Copy to client/.env.local for local development overrides

# Backend API Base URL (leave empty in local development to use the Vite proxy at /api)
VITE_API_BASE_URL=

# Socket.io Server URL (leave empty in local development to connect to the current origin)
VITE_SOCKET_URL=
